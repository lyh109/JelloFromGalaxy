enum SceneKeys
{
    Preloader = 'preloader',
    Title = 'title',
    CharacterSelect = 'character-select',
    Game = 'game',
    Pause = 'pause',
    GameOver = 'game-over'
}

export default SceneKeys